version: "3.8"

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./chroma_db:/app/chroma_db
      - ./server/conversations.db:/app/server/conversations.db
    restart: unless-stopped
